# Tiny Tapeout project information
# R2 latch-array + lane smoke test: branch spike/r2-latch only, never merged (DECISIONS D-032).
project:
  title:        "TRIPWIRE R2: lane with latch slot array"      # Project title
  author:       "Kanishk, Krithik"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "One TRIPWIRE lane (12 reflex slots in a latch array, EVAL/EXEC, ALU) driven and observed from the pins"      # One line description of what your project does
  language:     "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     50000000       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "3x2"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_tripwire"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "tt_um_tripwire.v"
    - "trw_lane.v"
    - "trw_alu.v"
    - "trw_slots.v"
    - "trw_cport.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "REG_WR"
  ui[1]: "REGSEL[0]"
  ui[2]: "REGSEL[1]"
  ui[3]: "REGSEL[2]"
  ui[4]: "SLOT_WR"
  ui[5]: "PUSH (I0)"
  ui[6]: "POP0 (O0)"
  ui[7]: "POP1 (O1)"

  # Outputs
  uo[0]: "OUT[0] (by RSEL: slot word, O0, O1, status)"
  uo[1]: "OUT[1]"
  uo[2]: "OUT[2]"
  uo[3]: "OUT[3]"
  uo[4]: "OUT[4]"
  uo[5]: "OUT[5]"
  uo[6]: "OUT[6]"
  uo[7]: "OUT[7]"

  # Bidirectional pins
  uio[0]: "DIN[0] (register data)"
  uio[1]: "DIN[1]"
  uio[2]: "DIN[2]"
  uio[3]: "DIN[3]"
  uio[4]: "DIN[4]"
  uio[5]: "DIN[5]"
  uio[6]: "DIN[6]"
  uio[7]: "DIN[7]"

# Do not change!
yaml_version: 6

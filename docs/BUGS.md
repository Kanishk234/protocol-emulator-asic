# TRIPWIRE bug ledger

Every bug found by any layer, newest at the bottom. Nothing is fixed silently.

| # | Date | Block | Symptom | Root cause | Caught by (check ID) | Now covered by | Fix commit |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-23 | `test/Makefile` (from the TT cmos5l template) | `gl_test` in gds run 35821375890 fails at elaboration: `Unknown module type: ihp_dff_r` (8 refs, one per counter flop) | In the pinned IHP-Open-PDK (rev 2bbec75), the flip-flop UDPs (`ihp_dff_r`, …) live in `sg13cmos5l_stdcell/verilog/sg13cmos5l_udp.v`, which the template's gate-level source list omits. Upstream template `cmos5l` has the same omission | L8-GL (`gl_test`) | L8-GL in CI, plus `scripts/gl_local.sh` (Yosys netlist on `sg13cmos5l_stdcell_typ_1p20V_25C.lib` + TT Icarus 13): without the UDP line it fails with the same error, with it 4/4 pass | 56a45ce (confirmed by gds run 35824649426, `gl_test` green) |
| 2 | 2026-09-23 | Spec: fabric tap semantics (`ARCHITECTURE.md` §4.3) | A tap that misses two tokens in a row stops seeing new tokens: 5 loads with no take gave 2 drops instead of 4, and the newest token was invisible | `last_seq`/`seq` are 1 bit. After two missed loads, `seq` is back to the tap's `last_seq`, so the new token looks already taken. Blocking subscribers cannot hit this, because the producer waits for them | L1-CHAN (`tools/tripsim/tests/test_fabric.py::test_tap_never_blocks_and_counts_drops`) | Same test; spec rule §14 F4: a counted drop also sets `last_seq := old seq`. An injected revert fails the test | pending (tripsim) |
| 3 | 2026-09-23 | tripsim pin unit: linked TX preload (new in D-011) | SPI controller at SCK periods 50 and 10: the second byte of a transfer read back wrong (bit 7 lost); period 6 passed by luck of timing | When the next byte's token arrived before the previous byte's final SCK fall, the preload and the "next bit on the fall" landed on the same edge, and the fall won | L3-SPI-C (`tools/kernels/tests/test_spi_controller.py::test_spi_controller_mode0`) | Same test at 3 SCK periods; rule §14 P9 (preload only when idle). An injected revert fails 4 tests | pending (tripsim) |
| 4 | 2026-09-23 | Firmware: I2C controller kernel (START sequence) | Every I2C controller test hung at the second START: SCL never started clocking after the START | The kernel made SDA fall, then sent SCL's "WAIT for SDA to fall". When SDA fell first (right after a STOP), the WAIT missed the edge and waited forever. A general rule the kernel broke: arm a WAIT before triggering its edge | L3-I2C-C (`tools/kernels/tests/test_i2c_controller.py`) | Same tests. Kernel now arms SCL's WAIT first; the STOP routine adds bus-free time (tBUF); rule written into §14 P16. Honest note: with tBUF in place, the reversed order no longer fails the current tests (an equivalent injection), so the ordering is kept for robustness, not because a test pins it | pending (tools) |
| 5 | 2026-09-23 | tripsim tooling: `vcd.py` (VCD writer) | sigrok decoded all UART bytes but the last (`TRIPWIRE\x00\xffU` without the final `0xAA`), while our reference decoder saw all of them | The VCD ended one clock after the last *change*. A final steady level (the last stop bit) was never in the file, so sigrok could not sample it | L3-UART (`tools/kernels/tests/test_uart.py::test_uart_tx`, sigrok leg) | Same test. The VCD now ends at the last *observed* clock | pending (tools) |
| 6 | 2026-09-23 | External tool: sigrok 0.5 `ps2` decoder | sigrok decoded device-to-host PS/2 frames as `1c f8 87 4b b0` while the chip and the reference device agreed on `1c f0 1c 5a 00 ff` | The decoder only finishes a frame on a 12th CLK fall (`bitcount == 11` is tested after appending), so it takes the next frame's start bit. Not our bug; our frames are correct | L3-PS2 (`tools/kernels/tests/test_ps2.py`, sigrok leg) | The test's recorded clk gets a short extra low pulse (DATA idle high) after each device frame, visible only to sigrok; comment in the test | pending (tools) |
| 7 | 2026-09-23 | Reference model: PS/2 device (`tools/protomodels/ps2.py`) | Interleaved test: the device never received the host's byte, then (after a first fix) received extra 0xFF bytes | (a) While waiting for bus idle before sending, the model missed the host's inhibit and never treated "CLK high, DATA low" as a request to send. (b) Right after its own ACK it read the bus one step late and saw its own DATA-low as a new request | L3-PS2 (`test_both_directions_interleaved`, `test_host_to_device`) | Same tests: the idle wait aborts on any bus activity, an RTS is recognised whenever idle, and the model yields once after each frame | pending (tools) |
| 8 | 2026-09-23 | Firmware: SWD program (`programs/swd.trw`, before commit) | The transaction after a write got a garbage request/ACK word and the bus had 74 clocks of contention | The lane returned to IDLE while the write's clocks were still running; SWDIO's RX framing kept emitting (echo) words that waited in `L0.I1` and were taken by the next transaction's ACK slot | L3-SWD (`tools/kernels/tests/test_swd.py`, contention counter + ACK checks) | Same tests. `rx_echo=false` plus a framing restart for every phase, so no stale word reaches the lane. Injected reverts (echo kept; no ACK restart; no WAIT before releasing SWDIO) each fail the tests | pending (programs) |
| 9 | 2026-09-23 | Test design: SAMPLE unit test | `test_sample_reads_pin_a_at_timed_points` got 0b110 instead of 0b101 | My waveform put the first sample (about clock 23) inside the low region; the model was right | L1-PIN (the test itself) | Waveform fixed so each sample lands mid-level | pending (tools) |
| 10 | 2026-09-23 | Test harness: `test_can.py` | Every CAN test lost the last frame on our side; L1 stalled with OVERRUN set | The test host never read HOST_OUT; the 16-deep host FIFO filled and back-pressure stopped the lane | L3-CAN (the tests themselves) | The harness reads HOST_OUT every clock, like a real host | pending (tools) |
| 11 | 2026-09-23 | Firmware design: CAN program (before commit) | A frame after one of ours was cut to 34 bits | L1's `FRAME` for our own frame waited behind TX tokens in L0's output register and arrived during the next frame, where it applied as "current" | L3-CAN (`test_we_send_reference_receives`) | `FRAME` [11] "next frame" vs current; a late current `FRAME` only sets LATE (§14 P24); program order (header first, the rest after the start event) | pending (programs, tripsim) |
| 12 | 2026-09-23 | Firmware design: CAN program (before commit) | Arbitration test: the reference's frame, received while ours was pending, was not ACKed and never terminated | While our frame waited for the bus, L0's output register held our next TX token, which the unit could not take; L1's FRAME/ACK commands queued behind it | L3-CAN (`test_arbitration_lost_then_retry`) | The unit emits "our frame started" (EVENT `0x9001`); L0 sends only the first token before it. The ACK became a queue-bypassing one-bit override (§14 P26) | pending (programs, tripsim) |
| 13 | 2026-09-23 | Firmware: CAN program (before commit) | Our frame never went past its header | A slot tested f1 right after the CMPM that wrote it; while f1 was pending, a flag-free "any TX result" slot fired first and consumed the start event | L3-CAN | The events carry "started" in data[0], tested with `head0`; no flag round trip | pending (programs) |
| 14 | 2026-09-23 | Reference model: CAN node (`tools/protomodels/can.py`) | The reference reported "no ACK" for a frame whose CRC ended in 5 equal bits, although we ACKed in the right slot (sigrok: "ACK slot: ACK") | Its TX inserted the stuff bit after the CRC, but its RX counted that stuff bit as the CRC delimiter | L3-CAN (`test_ack_lands_in_the_slot_after_a_stuff_bit_following_the_crc`) + sigrok | Same test | pending (tools) |
| 15 | 2026-09-23 | tripsim BITSYNC (`bitsync.py`, before commit) | Found by mutation review: removing the post-CRC stuff check changed no test | The ACK override was armed by any sample, so after a CRC ending in 5 equal bits it would have been armed by the stuff bit, putting our ACK in the CRC delimiter | Mutation review (L1) | The override is armed only by non-stuff samples (§14 P26); #14's scenario at two loop delays; both mutations now fail 2 tests | pending (tripsim) |
| 16 | 2026-09-23 | External tool: sigrok 0.5 `dmx512` decoder | sigrok lost byte alignment on our DMX output and never found the second packet | (a) Its last sample of a slot falls exactly on the end of the 2nd stop bit, so a legal zero MARK-between-frames (or a BREAK right after a slot) misaligns it; (b) a stop-bit error at bit 10 sets "find break" and is overwritten by "next frame" in the same step, so it only looks for a BREAK after 512 slots | L3-DMX (sigrok leg) | Our reference decoder checks zero MTBF/MTBP; the sigrok leg uses STOPS = 3 and MTBP = 20 µs and checks the first packet | n/a (external) |
| 17 | 2026-09-23 | tripc: routine worst-case bound | The LIN receive routine (69 words) was reported as 5 steps | The bound followed the straight-line path and stopped at the first RET, so early returns hid longer paths | Review of the tripc report | `test_routine_bound_is_the_longest_path_not_the_first_ret`; the bound is now the longest path through the branch graph | pending (tools) |
| 18 | 2026-09-23 | Firmware: servo program | A 19.5 ms or 20.25 ms frame when the host changed the width | The frame routine read the width twice, once for the high time and once for the rest of the 20 ms, and the host could change it in between (the routine waits on the unit for a whole frame) | L3-SERVO (`test_servo_widths_and_period`, period set) | One copy per frame; same test | pending (programs) |
| 19 | 2026-09-23 | Firmware: IR NEC program | Back-to-back frames merged the next leader mark into the stop mark; then a 108.56 ms period | No gap between frames; then padding computed from a 67.5 ms frame that is really 68.0625 ms with the final mark | L3-IR (reference decoder + period check) | Pads to exactly 108 ms; the test checks the period to the clock | pending (programs) |
| 20 | 2026-09-23 | Firmware: CAN program | Error-state reports of 0x9000 ("state" = the FRAME constant) | A slot loaded the next unit token into r1 while the previous routine still used r1 and then overwrote it | L3-CAN-ERR (state sequence) | Slots that load a routine's register wait for rb == 0 (all four in L1) | pending (programs) |
| 21 | 2026-09-23 | Firmware: CAN program | TEC never went down after an ACKed frame; bus-off not reached | The ACK result's level bit was tested with `shr 14` while bit 15 (the status marker) is also set | L3-CAN-ERR (bus-off test) | `(r1 >> 14) & 1`; same test | pending (programs) |
| 22 | 2026-09-23 | tripc: `ld`/`st` | `ld r2, r3, SETUP + 1` loaded offset 16 | Only the first token of the offset expression was evaluated; the rest was silently dropped | L3-USB (setup request never recognised) | `test_ld_st_offset_is_a_whole_expression` | pending (tools) |
| 23 | 2026-09-23 | Firmware: LIN program (before commit) | The next header was sent during the responder's answer | LIN is schedule based: the commander must keep each frame slot; the first version did not | L3-LIN | Slots paced in the pin unit by a delayed no-change LEVEL at the end of each slot (table from LIN's 1.4 x rule) | pending (programs) |
| 24 | 2026-09-23 | Reference model: I2S ADC | Received ADC words one bit late | The model looked for a WS change on the SCK fall; the Philips spec latches WS on the rise (the controller changes it just after the fall) | L3-I2S (loopback was right, ADC wrong) | Model latches WS on the rise | pending (tools) |
| 25 | 2026-09-23 | tripsim BITSYNC (before commit) | HDLC: a spurious abort after each frame; our first TX bit 3 clocks long | (a) After a closing flag the idle ones were counted as an abort of the held-back bits; (b) the hard sync on the echo of our own first bit restarted the bit | L3-HDLC | Abort only for a frame with committed bits; an own-echo start keeps our timing (§14 P21, P27) | pending (tripsim) |
| 26 | 2026-09-23 | tripsim BITSYNC (before commit) | USB packets cut after 11 bits | "Bus idle ends the frame" fired on NRZI 1s (runs of the idle level) | L3-USB | With DELIM = se0 only SE0 ends a frame (§14 P29) | pending (tripsim) |
| 27 | 2026-09-23 | tripsim BITSYNC (before commit) | A 1-bit release before our EOP | Processing the SE0 marker left that bit released; the SE0 items went out one bit later | L3-USB (timeline) | SE0/J items are driven in the same bit (after a due stuff bit) | pending (tripsim) |
| 28 | 2026-09-23 | Firmware: USB program (before commit) | SYNC/PID bytes stored as setup bytes; wrong clamps; wrong CRC-5 of a new address | `ltu` sets RZ when A < B: three branches were inverted; a `shr` between a test and its branch overwrote RZ | L3-USB | Same tests (GET_DESCRIPTOR at the new address) | pending (programs) |
| 29 | 2026-09-23 | tripsim BITSYNC (before commit) | sigrok decoded our USB data packets shifted (`92 04 20 ...`) while our host model accepted them | The unit resynced to the echo of its own transitions (the own-edge rule only covered the dominant level), stretching bits by up to SJW; our host model re-anchors on every edge and hid it | L3-USB sigrok leg (an independent oracle) | Never resync on an edge to the level we drive (§14 P22); the sigrok leg now checks every payload byte | pending (tripsim) |
| 30 | 2026-09-23 | External tool: sigrok 0.5 `can` decoder | After a remote frame with DLC > 0 (11- or 29-bit) sigrok read "ACK slot: NACK" and lost sync | It treats a remote frame's DLC as a data length | L3-CAN-ERR (sigrok leg) | The sigrok leg uses DLC 0 for remote frames; `test_remote_frames_with_a_dlc` checks the rest with the reference node | n/a (external) |
| 31 | 2026-09-23 | Reference model: CAN node (first version) | Error flags and ACK errors missing; stuffing after the CRC not handled | Written for part A without error frames | L3-CAN-ERR | Rewritten from the Bosch spec with part B, error flags and retransmission | pending (tools) |
| 32 | 2026-09-24 | tripsim lane: CALL with an output DST | (latent; no program emits it) A CALL slot with DST = O0/O1 would wait for that output and reserve it forever, since CALL's EXEC never loads it | EVAL treated every O0/O1 DST alike; the spec did not say what CALL does with DST | Spec review of R1 gap G6 (D-033) | §14 L10; `test_lane.py::test_call_ignores_dst` | pending (tripsim) |
| 33 | 2026-09-24 | tripc loader | (latent in the model) After loading a program with fewer than 12 slots, the unused slots keep whatever the latches held; on silicon, after power-up or a previous program, they could fire | `tripc.load` wrote only the compiled slots; the model's slots start with V = 0, which hid it | Spec review of R1 gap G7 (D-033) | §14 H1; `test_tripc.py::test_load_writes_every_slot` | pending (tripc) |
| 34 | 2026-09-24 | R1 spike lane (`spikes/r1_lane/trw_lane.v`) | (latent; no program emits these) (a) A slot with KT = 1 and ASRC not I0/I1 emitted the tag latched from an input head instead of OT. (b) A CALL slot with DST = O0/O1 waited for that output, reserved it at EVAL and loaded it at EXEC; with DFE = 1 it set PEND and wrote f[DF] (R = 0) | The spike's own answers to spec gap G6. §14 L8 and L10 (D-033) later fixed the rules: KT applies only when A is an input; CALL ignores DST and DFE. (b) is the spike's twin of the model's BUGS #32 | Spec review of R1 gap G6 (D-033), then checking the spike against L8/L10 | `tb_r1_lane.v`, run by `run_r1.sh`: KT with A = I0 and OT = ERR keeps DATA; KT with A = zero gives OT = EVENT. A CALL with DST = O0 while O0 holds an untaken byte, and DFE on f0 = 1: it must fire; O0 is unchanged; resv and PEND stay 0 in its own EXEC clock; f0 stays 1. Each of the 5 L10 guards removed alone, and the old KT rule, makes the tb fail. The tb also checks H1: no activity while halted (a lane ignoring RUN fails it) | pending (spikes) |
| 35 | 2026-09-24 | tripsim pin unit (`pinunit.py` `_emit`) | (latent; no program hits it) A SAMPLE command whose word completes in the same clock as a LINKED_RX word, or as an EVENT, crashes the model: `U0.rx: two loads in one clock` (for example SAMPLE delays 5, 11, 17 with pin B toggling every 3 clocks) | `_emit` checks `free()` (registered state) but not whether this clock already loaded, so the second word calls `load()` again. The spec (§14 P19: "both words are emitted") promises something a depth-1 producer cannot do | §14 second-person review, scratch probe (D-035 G15) | §14 P19 (D-035 F); `test_semantics.py` (one RX load per clock) | pending (tripsim, D-035) |
| 36 | 2026-09-24 | tripsim pin unit (`_shift_rx`, §14 P15) | (latent; pin C is only used with linked modes today) SHIFT_RX with pin C: a deselect in the middle of a word stops the receiver permanently. Two later full frames gave no tokens; without the deselect all words arrive | Deselect clears `_rx_bits` but leaves `_rx = "shift"` with the old `t0`. Sample i = 0 is then due in the past, and `now == …` never matches again | §14 second-person review, scratch probe with a control run (D-035 G16) | §14 P15 (D-035 G); `test_semantics.py` (deselect re-arms SHIFT_RX) | pending (tripsim, D-035) |
| 37 | 2026-09-24 | tripsim pin unit (`pad_drive`) | (latent; no program combines them) With CARRIER > 0 and C_OE, a deselected unit still drives pin A (OE = 1) while its level is active; without the carrier it correctly releases (OE = 0) | The carrier branch returns before the C_OE check | §14 second-person review, scratch probe with a control run (D-035 G17) | §14 P15 (D-035 H); `test_semantics.py` (C_OE checked before the carrier) | pending (tripsim, D-035) |
| 38 | 2026-09-24 | tripsim BITSYNC (`accept`, `SETN` rx) | (latent; no program sends n > 16) `SETN` rx with n = 17–31 set a 17–31-bit RX word, so a word could exceed 16 bits | BITSYNC used `(arg & 0x1F) or 16`, not the pin unit's rule (n = 0 or 17–31 means 16) | Line-by-line pass over §14 P20–P29 (D-035 outcome) | §14 P4; `test_semantics.py::test_bitsync_setn_rx_out_of_range_means_16` | pending (tripsim) |
| 39 | 2026-09-24 | tripsim BITSYNC (flag close, SE0 end) | (latent; no test checked it) A frame-end EVENT from a closing flag (DELIM = flag) or from SE0 (DELIM = se0) had data[14] = 0 even for our own frame | Only the `FRAME n` path added `own << 14`; P24 says every frame-end EVENT carries it | Line-by-line pass over §14 P20–P29 (D-035 outcome) | §14 P24; `test_semantics.py::test_bitsync_frame_end_event_carries_own_bit` | pending (tripsim) |

Note on #1: gate-level simulation also needs Tiny Tapeout's Icarus 13 build, which is what CI installs. Stock Icarus 12 leaves the cell models' `delayed_*` timing-check nets undriven, so every flop reads X. Use the TT build for any local gate-level run.

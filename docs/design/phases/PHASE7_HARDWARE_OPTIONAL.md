# Phase 7 (optional): Hardware

**Off the critical path.** Never let this phase delay phases 0–6.

## Options
- [ ] FPGA prototype: synthesize the shell and the generated fabric RTL onto a commercial FPGA board, load real bitstreams through the host interface, and capture protocol traffic with a logic analyzer against real devices. Label results as "FPGA prototype", never as silicon.
- [ ] Post-tapeout bring-up plan (if selected): test order, host scripts, expected results, what to measure first.

## Exit checklist (if attempted)
- [ ] Results recorded in `docs/reports/` with the exact commit and board
- [ ] `docs/CLAIMS.md` updated with correctly labeled hardware evidence
- [ ] `docs/summaries/PHASE7.md` written
